// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.impact.implementation;

import com.azure.core.http.rest.PagedIterable;
import com.azure.core.http.rest.Response;
import com.azure.core.http.rest.SimpleResponse;
import com.azure.core.util.Context;
import com.azure.core.util.logging.ClientLogger;
import com.azure.resourcemanager.impact.fluent.TopologyImpactsClient;
import com.azure.resourcemanager.impact.fluent.models.TopologyImpactInner;
import com.azure.resourcemanager.impact.models.TopologyImpact;
import com.azure.resourcemanager.impact.models.TopologyImpacts;

public final class TopologyImpactsImpl implements TopologyImpacts {
    private static final ClientLogger LOGGER = new ClientLogger(TopologyImpactsImpl.class);

    private final TopologyImpactsClient innerClient;

    private final com.azure.resourcemanager.impact.ImpactManager serviceManager;

    public TopologyImpactsImpl(TopologyImpactsClient innerClient,
        com.azure.resourcemanager.impact.ImpactManager serviceManager) {
        this.innerClient = innerClient;
        this.serviceManager = serviceManager;
    }

    public PagedIterable<TopologyImpact> list() {
        PagedIterable<TopologyImpactInner> inner = this.serviceClient().list();
        return ResourceManagerUtils.mapPage(inner, inner1 -> new TopologyImpactImpl(inner1, this.manager()));
    }

    public PagedIterable<TopologyImpact> list(Context context) {
        PagedIterable<TopologyImpactInner> inner = this.serviceClient().list(context);
        return ResourceManagerUtils.mapPage(inner, inner1 -> new TopologyImpactImpl(inner1, this.manager()));
    }

    public Response<TopologyImpact> getWithResponse(String topologyImpactName, Context context) {
        Response<TopologyImpactInner> inner = this.serviceClient().getWithResponse(topologyImpactName, context);
        if (inner != null) {
            return new SimpleResponse<>(inner.getRequest(), inner.getStatusCode(), inner.getHeaders(),
                new TopologyImpactImpl(inner.getValue(), this.manager()));
        } else {
            return null;
        }
    }

    public TopologyImpact get(String topologyImpactName) {
        TopologyImpactInner inner = this.serviceClient().get(topologyImpactName);
        if (inner != null) {
            return new TopologyImpactImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    public Response<Void> deleteWithResponse(String topologyImpactName, Context context) {
        return this.serviceClient().deleteWithResponse(topologyImpactName, context);
    }

    public void delete(String topologyImpactName) {
        this.serviceClient().delete(topologyImpactName);
    }

    public TopologyImpact getById(String id) {
        String topologyImpactName = ResourceManagerUtils.getValueFromIdByName(id, "topologyImpacts");
        if (topologyImpactName == null) {
            throw LOGGER.logExceptionAsError(new IllegalArgumentException(
                String.format("The resource ID '%s' is not valid. Missing path segment 'topologyImpacts'.", id)));
        }
        return this.getWithResponse(topologyImpactName, Context.NONE).getValue();
    }

    public Response<TopologyImpact> getByIdWithResponse(String id, Context context) {
        String topologyImpactName = ResourceManagerUtils.getValueFromIdByName(id, "topologyImpacts");
        if (topologyImpactName == null) {
            throw LOGGER.logExceptionAsError(new IllegalArgumentException(
                String.format("The resource ID '%s' is not valid. Missing path segment 'topologyImpacts'.", id)));
        }
        return this.getWithResponse(topologyImpactName, context);
    }

    public void deleteById(String id) {
        String topologyImpactName = ResourceManagerUtils.getValueFromIdByName(id, "topologyImpacts");
        if (topologyImpactName == null) {
            throw LOGGER.logExceptionAsError(new IllegalArgumentException(
                String.format("The resource ID '%s' is not valid. Missing path segment 'topologyImpacts'.", id)));
        }
        this.deleteWithResponse(topologyImpactName, Context.NONE);
    }

    public Response<Void> deleteByIdWithResponse(String id, Context context) {
        String topologyImpactName = ResourceManagerUtils.getValueFromIdByName(id, "topologyImpacts");
        if (topologyImpactName == null) {
            throw LOGGER.logExceptionAsError(new IllegalArgumentException(
                String.format("The resource ID '%s' is not valid. Missing path segment 'topologyImpacts'.", id)));
        }
        return this.deleteWithResponse(topologyImpactName, context);
    }

    private TopologyImpactsClient serviceClient() {
        return this.innerClient;
    }

    private com.azure.resourcemanager.impact.ImpactManager manager() {
        return this.serviceManager;
    }

    public TopologyImpactImpl define(String name) {
        return new TopologyImpactImpl(name, this.manager());
    }
}
