// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.ai.implementation;

import com.azure.core.management.SystemData;
import com.azure.core.util.Context;
import com.azure.resourcemanager.ai.fluent.models.ApplicationInner;
import com.azure.resourcemanager.ai.models.Application;
import com.azure.resourcemanager.ai.models.ApplicationArtifact;
import com.azure.resourcemanager.ai.models.ApplicationAuthorization;
import com.azure.resourcemanager.ai.models.ApplicationBillingDetailsDefinition;
import com.azure.resourcemanager.ai.models.ApplicationJitAccessPolicy;
import com.azure.resourcemanager.ai.models.ApplicationManagementMode;
import com.azure.resourcemanager.ai.models.ApplicationPackageContact;
import com.azure.resourcemanager.ai.models.ApplicationPackageSupportUrls;
import com.azure.resourcemanager.ai.models.ApplicationPatchable;
import com.azure.resourcemanager.ai.models.Identity;
import com.azure.resourcemanager.ai.models.Plan;
import com.azure.resourcemanager.ai.models.PlanPatchable;
import com.azure.resourcemanager.ai.models.ProvisioningState;
import com.azure.resourcemanager.ai.models.Sku;
import java.util.Collections;
import java.util.List;

public final class ApplicationImpl implements Application, Application.Definition, Application.Update {
    private ApplicationInner innerObject;

    private final com.azure.resourcemanager.ai.AiManager serviceManager;

    public String id() {
        return this.innerModel().id();
    }

    public String name() {
        return this.innerModel().name();
    }

    public String type() {
        return this.innerModel().type();
    }

    public String managedBy() {
        return this.innerModel().managedBy();
    }

    public Sku sku() {
        return this.innerModel().sku();
    }

    public SystemData systemData() {
        return this.innerModel().systemData();
    }

    public Plan plan() {
        return this.innerModel().plan();
    }

    public String kind() {
        return this.innerModel().kind();
    }

    public Identity identity() {
        return this.innerModel().identity();
    }

    public String managedResourceGroupId() {
        return this.innerModel().managedResourceGroupId();
    }

    public String applicationDefinitionId() {
        return this.innerModel().applicationDefinitionId();
    }

    public Object parameters() {
        return this.innerModel().parameters();
    }

    public Object outputs() {
        return this.innerModel().outputs();
    }

    public ProvisioningState provisioningState() {
        return this.innerModel().provisioningState();
    }

    public ApplicationBillingDetailsDefinition billingDetails() {
        return this.innerModel().billingDetails();
    }

    public ApplicationJitAccessPolicy jitAccessPolicy() {
        return this.innerModel().jitAccessPolicy();
    }

    public String publisherTenantId() {
        return this.innerModel().publisherTenantId();
    }

    public List<ApplicationAuthorization> authorizations() {
        List<ApplicationAuthorization> inner = this.innerModel().authorizations();
        if (inner != null) {
            return Collections.unmodifiableList(inner);
        } else {
            return Collections.emptyList();
        }
    }

    public ApplicationManagementMode managementMode() {
        return this.innerModel().managementMode();
    }

    public ApplicationPackageContact customerSupport() {
        return this.innerModel().customerSupport();
    }

    public ApplicationPackageSupportUrls supportUrls() {
        return this.innerModel().supportUrls();
    }

    public List<ApplicationArtifact> artifacts() {
        List<ApplicationArtifact> inner = this.innerModel().artifacts();
        if (inner != null) {
            return Collections.unmodifiableList(inner);
        } else {
            return Collections.emptyList();
        }
    }

    public String resourceGroupName() {
        return resourceGroupName;
    }

    public ApplicationInner innerModel() {
        return this.innerObject;
    }

    private com.azure.resourcemanager.ai.AiManager manager() {
        return this.serviceManager;
    }

    private String resourceGroupName;

    private String applicationName;

    private String location;

    private ApplicationPatchable updateParameters;

    public ApplicationImpl withExistingLocation(String resourceGroupName, String location) {
        this.resourceGroupName = resourceGroupName;
        this.location = location;
        return this;
    }

    public Application create() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getApplications()
                .createOrUpdate(resourceGroupName, applicationName, location, this.innerModel(), Context.NONE);
        return this;
    }

    public Application create(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getApplications()
                .createOrUpdate(resourceGroupName, applicationName, location, this.innerModel(), context);
        return this;
    }

    ApplicationImpl(String name, com.azure.resourcemanager.ai.AiManager serviceManager) {
        this.innerObject = new ApplicationInner();
        this.serviceManager = serviceManager;
        this.applicationName = name;
    }

    public ApplicationImpl update() {
        this.updateParameters = new ApplicationPatchable();
        return this;
    }

    public Application apply() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getApplications()
                .updateWithResponse(resourceGroupName, applicationName, location, updateParameters, Context.NONE)
                .getValue();
        return this;
    }

    public Application apply(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getApplications()
                .updateWithResponse(resourceGroupName, applicationName, location, updateParameters, context)
                .getValue();
        return this;
    }

    ApplicationImpl(ApplicationInner innerObject, com.azure.resourcemanager.ai.AiManager serviceManager) {
        this.innerObject = innerObject;
        this.serviceManager = serviceManager;
        this.resourceGroupName = Utils.getValueFromIdByName(innerObject.id(), "resourceGroups");
        this.applicationName = Utils.getValueFromIdByName(innerObject.id(), "applications");
        this.location = Utils.getValueFromIdByName(innerObject.id(), "locations");
    }

    public Application refresh() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getApplications()
                .getWithResponse(resourceGroupName, applicationName, location, Context.NONE)
                .getValue();
        return this;
    }

    public Application refresh(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getApplications()
                .getWithResponse(resourceGroupName, applicationName, location, context)
                .getValue();
        return this;
    }

    public void refreshPermissions() {
        serviceManager.applications().refreshPermissions(resourceGroupName, applicationName, location);
    }

    public void refreshPermissions(Context context) {
        serviceManager.applications().refreshPermissions(resourceGroupName, applicationName, location, context);
    }

    public ApplicationImpl withKind(String kind) {
        if (isInCreateMode()) {
            this.innerModel().withKind(kind);
            return this;
        } else {
            this.updateParameters.withKind(kind);
            return this;
        }
    }

    public ApplicationImpl withManagedBy(String managedBy) {
        if (isInCreateMode()) {
            this.innerModel().withManagedBy(managedBy);
            return this;
        } else {
            this.updateParameters.withManagedBy(managedBy);
            return this;
        }
    }

    public ApplicationImpl withSku(Sku sku) {
        if (isInCreateMode()) {
            this.innerModel().withSku(sku);
            return this;
        } else {
            this.updateParameters.withSku(sku);
            return this;
        }
    }

    public ApplicationImpl withPlan(Plan plan) {
        this.innerModel().withPlan(plan);
        return this;
    }

    public ApplicationImpl withIdentity(Identity identity) {
        if (isInCreateMode()) {
            this.innerModel().withIdentity(identity);
            return this;
        } else {
            this.updateParameters.withIdentity(identity);
            return this;
        }
    }

    public ApplicationImpl withManagedResourceGroupId(String managedResourceGroupId) {
        if (isInCreateMode()) {
            this.innerModel().withManagedResourceGroupId(managedResourceGroupId);
            return this;
        } else {
            this.updateParameters.withManagedResourceGroupId(managedResourceGroupId);
            return this;
        }
    }

    public ApplicationImpl withApplicationDefinitionId(String applicationDefinitionId) {
        if (isInCreateMode()) {
            this.innerModel().withApplicationDefinitionId(applicationDefinitionId);
            return this;
        } else {
            this.updateParameters.withApplicationDefinitionId(applicationDefinitionId);
            return this;
        }
    }

    public ApplicationImpl withParameters(Object parameters) {
        if (isInCreateMode()) {
            this.innerModel().withParameters(parameters);
            return this;
        } else {
            this.updateParameters.withParameters(parameters);
            return this;
        }
    }

    public ApplicationImpl withOutputs(Object outputs) {
        if (isInCreateMode()) {
            this.innerModel().withOutputs(outputs);
            return this;
        } else {
            this.updateParameters.withOutputs(outputs);
            return this;
        }
    }

    public ApplicationImpl withJitAccessPolicy(ApplicationJitAccessPolicy jitAccessPolicy) {
        if (isInCreateMode()) {
            this.innerModel().withJitAccessPolicy(jitAccessPolicy);
            return this;
        } else {
            this.updateParameters.withJitAccessPolicy(jitAccessPolicy);
            return this;
        }
    }

    public ApplicationImpl withPlan(PlanPatchable plan) {
        this.updateParameters.withPlan(plan);
        return this;
    }

    private boolean isInCreateMode() {
        return this.innerModel().id() == null;
    }
}
