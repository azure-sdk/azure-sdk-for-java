// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.appcontainers.implementation;

import com.azure.core.management.Region;
import com.azure.core.management.SystemData;
import com.azure.core.util.Context;
import com.azure.resourcemanager.appcontainers.fluent.models.SessionPoolInner;
import com.azure.resourcemanager.appcontainers.models.PatchProperties;
import com.azure.resourcemanager.appcontainers.models.Secret;
import com.azure.resourcemanager.appcontainers.models.SessionPool;
import java.util.Collections;
import java.util.List;
import java.util.Map;

public final class SessionPoolImpl implements SessionPool, SessionPool.Definition, SessionPool.Update {
    private SessionPoolInner innerObject;

    private final com.azure.resourcemanager.appcontainers.ContainerAppsApiManager serviceManager;

    public String id() {
        return this.innerModel().id();
    }

    public String name() {
        return this.innerModel().name();
    }

    public String type() {
        return this.innerModel().type();
    }

    public String location() {
        return this.innerModel().location();
    }

    public Map<String, String> tags() {
        Map<String, String> inner = this.innerModel().tags();
        if (inner != null) {
            return Collections.unmodifiableMap(inner);
        } else {
            return Collections.emptyMap();
        }
    }

    public SystemData systemData() {
        return this.innerModel().systemData();
    }

    public String managedEnvironmentId() {
        return this.innerModel().managedEnvironmentId();
    }

    public Integer maxConcurrentSessions() {
        return this.innerModel().maxConcurrentSessions();
    }

    public List<Secret> secrets() {
        List<Secret> inner = this.innerModel().secrets();
        if (inner != null) {
            return Collections.unmodifiableList(inner);
        } else {
            return Collections.emptyList();
        }
    }

    public Region region() {
        return Region.fromName(this.regionName());
    }

    public String regionName() {
        return this.location();
    }

    public String resourceGroupName() {
        return resourceGroupName;
    }

    public SessionPoolInner innerModel() {
        return this.innerObject;
    }

    private com.azure.resourcemanager.appcontainers.ContainerAppsApiManager manager() {
        return this.serviceManager;
    }

    private String resourceGroupName;

    private String sessionPoolName;

    private PatchProperties updateSessionPoolNameEnvelope;

    public SessionPoolImpl withExistingResourceGroup(String resourceGroupName) {
        this.resourceGroupName = resourceGroupName;
        return this;
    }

    public SessionPool create() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContainerAppsSessionPools()
                .createOrUpdateWithResponse(resourceGroupName, sessionPoolName, this.innerModel(), Context.NONE)
                .getValue();
        return this;
    }

    public SessionPool create(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContainerAppsSessionPools()
                .createOrUpdateWithResponse(resourceGroupName, sessionPoolName, this.innerModel(), context)
                .getValue();
        return this;
    }

    SessionPoolImpl(String name, com.azure.resourcemanager.appcontainers.ContainerAppsApiManager serviceManager) {
        this.innerObject = new SessionPoolInner();
        this.serviceManager = serviceManager;
        this.sessionPoolName = name;
    }

    public SessionPoolImpl update() {
        this.updateSessionPoolNameEnvelope = new PatchProperties();
        return this;
    }

    public SessionPool apply() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContainerAppsSessionPools()
                .updateWithResponse(resourceGroupName, sessionPoolName, updateSessionPoolNameEnvelope, Context.NONE)
                .getValue();
        return this;
    }

    public SessionPool apply(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContainerAppsSessionPools()
                .updateWithResponse(resourceGroupName, sessionPoolName, updateSessionPoolNameEnvelope, context)
                .getValue();
        return this;
    }

    SessionPoolImpl(
        SessionPoolInner innerObject, com.azure.resourcemanager.appcontainers.ContainerAppsApiManager serviceManager) {
        this.innerObject = innerObject;
        this.serviceManager = serviceManager;
        this.resourceGroupName = Utils.getValueFromIdByName(innerObject.id(), "resourceGroups");
        this.sessionPoolName = Utils.getValueFromIdByName(innerObject.id(), "sessionPools");
    }

    public SessionPool refresh() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContainerAppsSessionPools()
                .getByResourceGroupWithResponse(resourceGroupName, sessionPoolName, Context.NONE)
                .getValue();
        return this;
    }

    public SessionPool refresh(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContainerAppsSessionPools()
                .getByResourceGroupWithResponse(resourceGroupName, sessionPoolName, context)
                .getValue();
        return this;
    }

    public SessionPoolImpl withRegion(Region location) {
        this.innerModel().withLocation(location.toString());
        return this;
    }

    public SessionPoolImpl withRegion(String location) {
        this.innerModel().withLocation(location);
        return this;
    }

    public SessionPoolImpl withTags(Map<String, String> tags) {
        this.innerModel().withTags(tags);
        return this;
    }

    public SessionPoolImpl withManagedEnvironmentId(String managedEnvironmentId) {
        this.innerModel().withManagedEnvironmentId(managedEnvironmentId);
        return this;
    }

    public SessionPoolImpl withMaxConcurrentSessions(Integer maxConcurrentSessions) {
        if (isInCreateMode()) {
            this.innerModel().withMaxConcurrentSessions(maxConcurrentSessions);
            return this;
        } else {
            this.updateSessionPoolNameEnvelope.withMaxConcurrentSessions(maxConcurrentSessions);
            return this;
        }
    }

    public SessionPoolImpl withSecrets(List<Secret> secrets) {
        if (isInCreateMode()) {
            this.innerModel().withSecrets(secrets);
            return this;
        } else {
            this.updateSessionPoolNameEnvelope.withSecrets(secrets);
            return this;
        }
    }

    private boolean isInCreateMode() {
        return this.innerModel().id() == null;
    }
}
