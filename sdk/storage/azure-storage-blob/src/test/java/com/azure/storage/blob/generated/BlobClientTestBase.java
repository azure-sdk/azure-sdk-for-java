// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) TypeSpec Code Generator.

package com.azure.storage.blob.generated;

// The Java test files under 'generated' package are generated for your reference.
// If you wish to modify these files, please copy them out of the 'generated' package, and modify there.
// See https://aka.ms/azsdk/dpg/java/tests for guide on adding a test.

import com.azure.core.http.policy.HttpLogDetailLevel;
import com.azure.core.http.policy.HttpLogOptions;
import com.azure.core.test.TestMode;
import com.azure.core.test.TestProxyTestBase;
import com.azure.core.test.utils.MockTokenCredential;
import com.azure.core.util.Configuration;
import com.azure.identity.DefaultAzureCredentialBuilder;
import com.azure.storage.blob.BlobClient;
import com.azure.storage.blob.BlobClientBuilder;
import com.azure.storage.blob.ContainerBlobAppendBlobClient;
import com.azure.storage.blob.ContainerBlobBlockBlobClient;
import com.azure.storage.blob.ContainerBlobClient;
import com.azure.storage.blob.ContainerBlobPageBlobClient;
import com.azure.storage.blob.ContainerClient;

class BlobClientTestBase extends TestProxyTestBase {
    protected BlobClient blobClient;

    protected ContainerClient containerClient;

    protected ContainerBlobClient containerBlobClient;

    protected ContainerBlobPageBlobClient containerBlobPageBlobClient;

    protected ContainerBlobAppendBlobClient containerBlobAppendBlobClient;

    protected ContainerBlobBlockBlobClient containerBlobBlockBlobClient;

    @Override
    protected void beforeTest() {
        BlobClientBuilder blobClientbuilder
            = new BlobClientBuilder().url(Configuration.getGlobalConfiguration().get("URL", "url"))
                .containerName(Configuration.getGlobalConfiguration().get("CONTAINERNAME", "containername"))
                .blobName(Configuration.getGlobalConfiguration().get("BLOBNAME", "blobname"))
                .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
                .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.PLAYBACK) {
            blobClientbuilder.credential(new MockTokenCredential());
        } else if (getTestMode() == TestMode.RECORD) {
            blobClientbuilder.addPolicy(interceptorManager.getRecordPolicy())
                .credential(new DefaultAzureCredentialBuilder().build());
        } else if (getTestMode() == TestMode.LIVE) {
            blobClientbuilder.credential(new DefaultAzureCredentialBuilder().build());
        }
        blobClient = blobClientbuilder.buildClient();

        BlobClientBuilder containerClientbuilder
            = new BlobClientBuilder().url(Configuration.getGlobalConfiguration().get("URL", "url"))
                .containerName(Configuration.getGlobalConfiguration().get("CONTAINERNAME", "containername"))
                .blobName(Configuration.getGlobalConfiguration().get("BLOBNAME", "blobname"))
                .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
                .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.PLAYBACK) {
            containerClientbuilder.credential(new MockTokenCredential());
        } else if (getTestMode() == TestMode.RECORD) {
            containerClientbuilder.addPolicy(interceptorManager.getRecordPolicy())
                .credential(new DefaultAzureCredentialBuilder().build());
        } else if (getTestMode() == TestMode.LIVE) {
            containerClientbuilder.credential(new DefaultAzureCredentialBuilder().build());
        }
        containerClient = containerClientbuilder.buildContainerClient();

        BlobClientBuilder containerBlobClientbuilder
            = new BlobClientBuilder().url(Configuration.getGlobalConfiguration().get("URL", "url"))
                .containerName(Configuration.getGlobalConfiguration().get("CONTAINERNAME", "containername"))
                .blobName(Configuration.getGlobalConfiguration().get("BLOBNAME", "blobname"))
                .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
                .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.PLAYBACK) {
            containerBlobClientbuilder.credential(new MockTokenCredential());
        } else if (getTestMode() == TestMode.RECORD) {
            containerBlobClientbuilder.addPolicy(interceptorManager.getRecordPolicy())
                .credential(new DefaultAzureCredentialBuilder().build());
        } else if (getTestMode() == TestMode.LIVE) {
            containerBlobClientbuilder.credential(new DefaultAzureCredentialBuilder().build());
        }
        containerBlobClient = containerBlobClientbuilder.buildContainerBlobClient();

        BlobClientBuilder containerBlobPageBlobClientbuilder
            = new BlobClientBuilder().url(Configuration.getGlobalConfiguration().get("URL", "url"))
                .containerName(Configuration.getGlobalConfiguration().get("CONTAINERNAME", "containername"))
                .blobName(Configuration.getGlobalConfiguration().get("BLOBNAME", "blobname"))
                .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
                .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.PLAYBACK) {
            containerBlobPageBlobClientbuilder.credential(new MockTokenCredential());
        } else if (getTestMode() == TestMode.RECORD) {
            containerBlobPageBlobClientbuilder.addPolicy(interceptorManager.getRecordPolicy())
                .credential(new DefaultAzureCredentialBuilder().build());
        } else if (getTestMode() == TestMode.LIVE) {
            containerBlobPageBlobClientbuilder.credential(new DefaultAzureCredentialBuilder().build());
        }
        containerBlobPageBlobClient = containerBlobPageBlobClientbuilder.buildContainerBlobPageBlobClient();

        BlobClientBuilder containerBlobAppendBlobClientbuilder
            = new BlobClientBuilder().url(Configuration.getGlobalConfiguration().get("URL", "url"))
                .containerName(Configuration.getGlobalConfiguration().get("CONTAINERNAME", "containername"))
                .blobName(Configuration.getGlobalConfiguration().get("BLOBNAME", "blobname"))
                .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
                .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.PLAYBACK) {
            containerBlobAppendBlobClientbuilder.credential(new MockTokenCredential());
        } else if (getTestMode() == TestMode.RECORD) {
            containerBlobAppendBlobClientbuilder.addPolicy(interceptorManager.getRecordPolicy())
                .credential(new DefaultAzureCredentialBuilder().build());
        } else if (getTestMode() == TestMode.LIVE) {
            containerBlobAppendBlobClientbuilder.credential(new DefaultAzureCredentialBuilder().build());
        }
        containerBlobAppendBlobClient = containerBlobAppendBlobClientbuilder.buildContainerBlobAppendBlobClient();

        BlobClientBuilder containerBlobBlockBlobClientbuilder
            = new BlobClientBuilder().url(Configuration.getGlobalConfiguration().get("URL", "url"))
                .containerName(Configuration.getGlobalConfiguration().get("CONTAINERNAME", "containername"))
                .blobName(Configuration.getGlobalConfiguration().get("BLOBNAME", "blobname"))
                .httpClient(getHttpClientOrUsePlayback(getHttpClients().findFirst().orElse(null)))
                .httpLogOptions(new HttpLogOptions().setLogLevel(HttpLogDetailLevel.BASIC));
        if (getTestMode() == TestMode.PLAYBACK) {
            containerBlobBlockBlobClientbuilder.credential(new MockTokenCredential());
        } else if (getTestMode() == TestMode.RECORD) {
            containerBlobBlockBlobClientbuilder.addPolicy(interceptorManager.getRecordPolicy())
                .credential(new DefaultAzureCredentialBuilder().build());
        } else if (getTestMode() == TestMode.LIVE) {
            containerBlobBlockBlobClientbuilder.credential(new DefaultAzureCredentialBuilder().build());
        }
        containerBlobBlockBlobClient = containerBlobBlockBlobClientbuilder.buildContainerBlobBlockBlobClient();

    }
}
