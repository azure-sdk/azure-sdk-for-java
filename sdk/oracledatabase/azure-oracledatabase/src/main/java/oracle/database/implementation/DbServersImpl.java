// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) TypeSpec Code Generator.

package oracle.database.implementation;

import com.azure.core.http.rest.PagedIterable;
import com.azure.core.http.rest.Response;
import com.azure.core.http.rest.SimpleResponse;
import com.azure.core.util.Context;
import com.azure.core.util.logging.ClientLogger;
import oracle.database.fluent.DbServersClient;
import oracle.database.fluent.models.DbServerInner;
import oracle.database.models.DbServer;
import oracle.database.models.DbServers;

public final class DbServersImpl implements DbServers {
    private static final ClientLogger LOGGER = new ClientLogger(DbServersImpl.class);

    private final DbServersClient innerClient;

    private final oracle.database.DatabaseManager serviceManager;

    public DbServersImpl(DbServersClient innerClient, oracle.database.DatabaseManager serviceManager) {
        this.innerClient = innerClient;
        this.serviceManager = serviceManager;
    }

    public Response<DbServer> getWithResponse(String resourceGroupName, String cloudexadatainfrastructurename,
        String dbserverocid, Context context) {
        Response<DbServerInner> inner = this.serviceClient()
            .getWithResponse(resourceGroupName, cloudexadatainfrastructurename, dbserverocid, context);
        if (inner != null) {
            return new SimpleResponse<>(inner.getRequest(), inner.getStatusCode(), inner.getHeaders(),
                new DbServerImpl(inner.getValue(), this.manager()));
        } else {
            return null;
        }
    }

    public DbServer get(String resourceGroupName, String cloudexadatainfrastructurename, String dbserverocid) {
        DbServerInner inner = this.serviceClient().get(resourceGroupName, cloudexadatainfrastructurename, dbserverocid);
        if (inner != null) {
            return new DbServerImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    public PagedIterable<DbServer> listByParent(String resourceGroupName, String cloudexadatainfrastructurename) {
        PagedIterable<DbServerInner> inner
            = this.serviceClient().listByParent(resourceGroupName, cloudexadatainfrastructurename);
        return ResourceManagerUtils.mapPage(inner, inner1 -> new DbServerImpl(inner1, this.manager()));
    }

    public PagedIterable<DbServer> listByParent(String resourceGroupName, String cloudexadatainfrastructurename,
        Context context) {
        PagedIterable<DbServerInner> inner
            = this.serviceClient().listByParent(resourceGroupName, cloudexadatainfrastructurename, context);
        return ResourceManagerUtils.mapPage(inner, inner1 -> new DbServerImpl(inner1, this.manager()));
    }

    private DbServersClient serviceClient() {
        return this.innerClient;
    }

    private oracle.database.DatabaseManager manager() {
        return this.serviceManager;
    }
}
