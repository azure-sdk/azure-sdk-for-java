// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.billing.implementation;

import com.azure.core.http.rest.PagedIterable;
import com.azure.core.http.rest.Response;
import com.azure.core.http.rest.SimpleResponse;
import com.azure.core.util.Context;
import com.azure.core.util.logging.ClientLogger;
import com.azure.resourcemanager.billing.fluent.EnrollmentAccountsClient;
import com.azure.resourcemanager.billing.fluent.models.EnrollmentAccountAutoGeneratedInner;
import com.azure.resourcemanager.billing.models.EnrollmentAccountAutoGenerated;
import com.azure.resourcemanager.billing.models.EnrollmentAccounts;

public final class EnrollmentAccountsImpl implements EnrollmentAccounts {
    private static final ClientLogger LOGGER = new ClientLogger(EnrollmentAccountsImpl.class);

    private final EnrollmentAccountsClient innerClient;

    private final com.azure.resourcemanager.billing.BillingManager serviceManager;

    public EnrollmentAccountsImpl(
        EnrollmentAccountsClient innerClient, com.azure.resourcemanager.billing.BillingManager serviceManager) {
        this.innerClient = innerClient;
        this.serviceManager = serviceManager;
    }

    public PagedIterable<EnrollmentAccountAutoGenerated> listByDepartmentName(
        String billingAccountName, String departmentName) {
        PagedIterable<EnrollmentAccountAutoGeneratedInner> inner =
            this.serviceClient().listByDepartmentName(billingAccountName, departmentName);
        return Utils.mapPage(inner, inner1 -> new EnrollmentAccountAutoGeneratedImpl(inner1, this.manager()));
    }

    public PagedIterable<EnrollmentAccountAutoGenerated> listByDepartmentName(
        String billingAccountName,
        String departmentName,
        String filter,
        String orderBy,
        Long top,
        Long skip,
        Boolean count,
        String search,
        Context context) {
        PagedIterable<EnrollmentAccountAutoGeneratedInner> inner =
            this
                .serviceClient()
                .listByDepartmentName(
                    billingAccountName, departmentName, filter, orderBy, top, skip, count, search, context);
        return Utils.mapPage(inner, inner1 -> new EnrollmentAccountAutoGeneratedImpl(inner1, this.manager()));
    }

    public Response<EnrollmentAccountAutoGenerated> getByAccountIdByDepartmentIdWithResponse(
        String billingAccountName, String departmentName, String enrollmentAccountName, Context context) {
        Response<EnrollmentAccountAutoGeneratedInner> inner =
            this
                .serviceClient()
                .getByAccountIdByDepartmentIdWithResponse(
                    billingAccountName, departmentName, enrollmentAccountName, context);
        if (inner != null) {
            return new SimpleResponse<>(
                inner.getRequest(),
                inner.getStatusCode(),
                inner.getHeaders(),
                new EnrollmentAccountAutoGeneratedImpl(inner.getValue(), this.manager()));
        } else {
            return null;
        }
    }

    public EnrollmentAccountAutoGenerated getByAccountIdByDepartmentId(
        String billingAccountName, String departmentName, String enrollmentAccountName) {
        EnrollmentAccountAutoGeneratedInner inner =
            this
                .serviceClient()
                .getByAccountIdByDepartmentId(billingAccountName, departmentName, enrollmentAccountName);
        if (inner != null) {
            return new EnrollmentAccountAutoGeneratedImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    public PagedIterable<EnrollmentAccountAutoGenerated> listByBillingAccount(String billingAccountName) {
        PagedIterable<EnrollmentAccountAutoGeneratedInner> inner =
            this.serviceClient().listByBillingAccount(billingAccountName);
        return Utils.mapPage(inner, inner1 -> new EnrollmentAccountAutoGeneratedImpl(inner1, this.manager()));
    }

    public PagedIterable<EnrollmentAccountAutoGenerated> listByBillingAccount(
        String billingAccountName,
        String filter,
        String orderBy,
        Long top,
        Long skip,
        Boolean count,
        String search,
        Context context) {
        PagedIterable<EnrollmentAccountAutoGeneratedInner> inner =
            this
                .serviceClient()
                .listByBillingAccount(billingAccountName, filter, orderBy, top, skip, count, search, context);
        return Utils.mapPage(inner, inner1 -> new EnrollmentAccountAutoGeneratedImpl(inner1, this.manager()));
    }

    public Response<EnrollmentAccountAutoGenerated> getWithResponse(
        String billingAccountName, String enrollmentAccountName, Context context) {
        Response<EnrollmentAccountAutoGeneratedInner> inner =
            this.serviceClient().getWithResponse(billingAccountName, enrollmentAccountName, context);
        if (inner != null) {
            return new SimpleResponse<>(
                inner.getRequest(),
                inner.getStatusCode(),
                inner.getHeaders(),
                new EnrollmentAccountAutoGeneratedImpl(inner.getValue(), this.manager()));
        } else {
            return null;
        }
    }

    public EnrollmentAccountAutoGenerated get(String billingAccountName, String enrollmentAccountName) {
        EnrollmentAccountAutoGeneratedInner inner = this.serviceClient().get(billingAccountName, enrollmentAccountName);
        if (inner != null) {
            return new EnrollmentAccountAutoGeneratedImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    private EnrollmentAccountsClient serviceClient() {
        return this.innerClient;
    }

    private com.azure.resourcemanager.billing.BillingManager manager() {
        return this.serviceManager;
    }
}
