// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.mission.implementation;

import com.azure.core.management.Region;
import com.azure.core.management.SystemData;
import com.azure.core.util.Context;
import com.azure.resourcemanager.mission.fluent.models.EndpointResourceInner;
import com.azure.resourcemanager.mission.models.EndpointResource;
import com.azure.resourcemanager.mission.models.EndpointResourceUpdate;
import com.azure.resourcemanager.mission.models.EndpointRule;
import com.azure.resourcemanager.mission.models.ProvisioningState;
import java.util.Collections;
import java.util.List;
import java.util.Map;

public final class EndpointResourceImpl
    implements EndpointResource, EndpointResource.Definition, EndpointResource.Update {
    private EndpointResourceInner innerObject;

    private final com.azure.resourcemanager.mission.MissionManager serviceManager;

    public String id() {
        return this.innerModel().id();
    }

    public String name() {
        return this.innerModel().name();
    }

    public String type() {
        return this.innerModel().type();
    }

    public String location() {
        return this.innerModel().location();
    }

    public Map<String, String> tags() {
        Map<String, String> inner = this.innerModel().tags();
        if (inner != null) {
            return Collections.unmodifiableMap(inner);
        } else {
            return Collections.emptyMap();
        }
    }

    public SystemData systemData() {
        return this.innerModel().systemData();
    }

    public List<EndpointRule> endpointRuleCollection() {
        List<EndpointRule> inner = this.innerModel().endpointRuleCollection();
        if (inner != null) {
            return Collections.unmodifiableList(inner);
        } else {
            return Collections.emptyList();
        }
    }

    public ProvisioningState provisioningState() {
        return this.innerModel().provisioningState();
    }

    public Region region() {
        return Region.fromName(this.regionName());
    }

    public String regionName() {
        return this.location();
    }

    public String resourceGroupName() {
        return resourceGroupName;
    }

    public EndpointResourceInner innerModel() {
        return this.innerObject;
    }

    private com.azure.resourcemanager.mission.MissionManager manager() {
        return this.serviceManager;
    }

    private String resourceGroupName;

    private String virtualEnclaveName;

    private String endpointName;

    private EndpointResourceUpdate updateProperties;

    public EndpointResourceImpl withExistingVirtualEnclave(String resourceGroupName, String virtualEnclaveName) {
        this.resourceGroupName = resourceGroupName;
        this.virtualEnclaveName = virtualEnclaveName;
        return this;
    }

    public EndpointResource create() {
        this.innerObject = serviceManager.serviceClient()
            .getEndpoints()
            .createOrUpdate(resourceGroupName, virtualEnclaveName, endpointName, this.innerModel(), Context.NONE);
        return this;
    }

    public EndpointResource create(Context context) {
        this.innerObject = serviceManager.serviceClient()
            .getEndpoints()
            .createOrUpdate(resourceGroupName, virtualEnclaveName, endpointName, this.innerModel(), context);
        return this;
    }

    EndpointResourceImpl(String name, com.azure.resourcemanager.mission.MissionManager serviceManager) {
        this.innerObject = new EndpointResourceInner();
        this.serviceManager = serviceManager;
        this.endpointName = name;
    }

    public EndpointResourceImpl update() {
        this.updateProperties = new EndpointResourceUpdate();
        return this;
    }

    public EndpointResource apply() {
        this.innerObject = serviceManager.serviceClient()
            .getEndpoints()
            .update(resourceGroupName, virtualEnclaveName, endpointName, updateProperties, Context.NONE);
        return this;
    }

    public EndpointResource apply(Context context) {
        this.innerObject = serviceManager.serviceClient()
            .getEndpoints()
            .update(resourceGroupName, virtualEnclaveName, endpointName, updateProperties, context);
        return this;
    }

    EndpointResourceImpl(EndpointResourceInner innerObject,
        com.azure.resourcemanager.mission.MissionManager serviceManager) {
        this.innerObject = innerObject;
        this.serviceManager = serviceManager;
        this.resourceGroupName = ResourceManagerUtils.getValueFromIdByName(innerObject.id(), "resourceGroups");
        this.virtualEnclaveName = ResourceManagerUtils.getValueFromIdByName(innerObject.id(), "virtualEnclaves");
        this.endpointName = ResourceManagerUtils.getValueFromIdByName(innerObject.id(), "endpoints");
    }

    public EndpointResource refresh() {
        this.innerObject = serviceManager.serviceClient()
            .getEndpoints()
            .getWithResponse(resourceGroupName, virtualEnclaveName, endpointName, Context.NONE)
            .getValue();
        return this;
    }

    public EndpointResource refresh(Context context) {
        this.innerObject = serviceManager.serviceClient()
            .getEndpoints()
            .getWithResponse(resourceGroupName, virtualEnclaveName, endpointName, context)
            .getValue();
        return this;
    }

    public EndpointResourceImpl withRegion(Region location) {
        this.innerModel().withLocation(location.toString());
        return this;
    }

    public EndpointResourceImpl withRegion(String location) {
        this.innerModel().withLocation(location);
        return this;
    }

    public EndpointResourceImpl withTags(Map<String, String> tags) {
        if (isInCreateMode()) {
            this.innerModel().withTags(tags);
            return this;
        } else {
            this.updateProperties.withTags(tags);
            return this;
        }
    }

    public EndpointResourceImpl withEndpointRuleCollection(List<EndpointRule> endpointRuleCollection) {
        if (isInCreateMode()) {
            this.innerModel().withEndpointRuleCollection(endpointRuleCollection);
            return this;
        } else {
            this.updateProperties.withEndpointRuleCollection(endpointRuleCollection);
            return this;
        }
    }

    private boolean isInCreateMode() {
        return this.innerModel().id() == null;
    }
}
