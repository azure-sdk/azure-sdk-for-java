// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) TypeSpec Code Generator.

package com.azure.developer.microsoftplaywrighttesting.implementation;

import com.azure.core.http.HttpPipeline;
import com.azure.core.http.HttpPipelineBuilder;
import com.azure.core.http.policy.RetryPolicy;
import com.azure.core.http.policy.UserAgentPolicy;
import com.azure.core.util.serializer.JacksonAdapter;
import com.azure.core.util.serializer.SerializerAdapter;
import com.azure.developer.microsoftplaywrighttesting.MicrosoftPlaywrightTestingServiceVersion;

/**
 * Initializes a new instance of the ReportingClient type.
 */
public final class ReportingClientImpl {
    /**
     * Supported Azure Playwright Service API Endpoints (protocol and hostname, for example:
     * https://{region}.reporting.api.playwright.microsoft.com).
     */
    private final String endpoint;

    /**
     * Gets Supported Azure Playwright Service API Endpoints (protocol and hostname, for example:
     * https://{region}.reporting.api.playwright.microsoft.com).
     * 
     * @return the endpoint value.
     */
    public String getEndpoint() {
        return this.endpoint;
    }

    /**
     * Service version.
     */
    private final MicrosoftPlaywrightTestingServiceVersion serviceVersion;

    /**
     * Gets Service version.
     * 
     * @return the serviceVersion value.
     */
    public MicrosoftPlaywrightTestingServiceVersion getServiceVersion() {
        return this.serviceVersion;
    }

    /**
     * The HTTP pipeline to send requests through.
     */
    private final HttpPipeline httpPipeline;

    /**
     * Gets The HTTP pipeline to send requests through.
     * 
     * @return the httpPipeline value.
     */
    public HttpPipeline getHttpPipeline() {
        return this.httpPipeline;
    }

    /**
     * The serializer to serialize an object into a string.
     */
    private final SerializerAdapter serializerAdapter;

    /**
     * Gets The serializer to serialize an object into a string.
     * 
     * @return the serializerAdapter value.
     */
    public SerializerAdapter getSerializerAdapter() {
        return this.serializerAdapter;
    }

    /**
     * The TestRunsImpl object to access its operations.
     */
    private final TestRunsImpl testRuns;

    /**
     * Gets the TestRunsImpl object to access its operations.
     * 
     * @return the TestRunsImpl object.
     */
    public TestRunsImpl getTestRuns() {
        return this.testRuns;
    }

    /**
     * The TestResultsImpl object to access its operations.
     */
    private final TestResultsImpl testResults;

    /**
     * Gets the TestResultsImpl object to access its operations.
     * 
     * @return the TestResultsImpl object.
     */
    public TestResultsImpl getTestResults() {
        return this.testResults;
    }

    /**
     * Initializes an instance of ReportingClient client.
     * 
     * @param endpoint Supported Azure Playwright Service API Endpoints (protocol and hostname, for example:
     * https://{region}.reporting.api.playwright.microsoft.com).
     * @param serviceVersion Service version.
     */
    public ReportingClientImpl(String endpoint, MicrosoftPlaywrightTestingServiceVersion serviceVersion) {
        this(new HttpPipelineBuilder().policies(new UserAgentPolicy(), new RetryPolicy()).build(),
            JacksonAdapter.createDefaultSerializerAdapter(), endpoint, serviceVersion);
    }

    /**
     * Initializes an instance of ReportingClient client.
     * 
     * @param httpPipeline The HTTP pipeline to send requests through.
     * @param endpoint Supported Azure Playwright Service API Endpoints (protocol and hostname, for example:
     * https://{region}.reporting.api.playwright.microsoft.com).
     * @param serviceVersion Service version.
     */
    public ReportingClientImpl(HttpPipeline httpPipeline, String endpoint,
        MicrosoftPlaywrightTestingServiceVersion serviceVersion) {
        this(httpPipeline, JacksonAdapter.createDefaultSerializerAdapter(), endpoint, serviceVersion);
    }

    /**
     * Initializes an instance of ReportingClient client.
     * 
     * @param httpPipeline The HTTP pipeline to send requests through.
     * @param serializerAdapter The serializer to serialize an object into a string.
     * @param endpoint Supported Azure Playwright Service API Endpoints (protocol and hostname, for example:
     * https://{region}.reporting.api.playwright.microsoft.com).
     * @param serviceVersion Service version.
     */
    public ReportingClientImpl(HttpPipeline httpPipeline, SerializerAdapter serializerAdapter, String endpoint,
        MicrosoftPlaywrightTestingServiceVersion serviceVersion) {
        this.httpPipeline = httpPipeline;
        this.serializerAdapter = serializerAdapter;
        this.endpoint = endpoint;
        this.serviceVersion = serviceVersion;
        this.testRuns = new TestRunsImpl(this);
        this.testResults = new TestResultsImpl(this);
    }
}
