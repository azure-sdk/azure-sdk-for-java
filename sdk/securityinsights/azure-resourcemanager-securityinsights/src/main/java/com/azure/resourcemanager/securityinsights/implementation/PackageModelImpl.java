// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.securityinsights.implementation;

import com.azure.core.management.SystemData;
import com.azure.core.util.Context;
import com.azure.resourcemanager.securityinsights.fluent.models.PackageModelInner;
import com.azure.resourcemanager.securityinsights.models.Flag;
import com.azure.resourcemanager.securityinsights.models.MetadataAuthor;
import com.azure.resourcemanager.securityinsights.models.MetadataCategories;
import com.azure.resourcemanager.securityinsights.models.MetadataDependencies;
import com.azure.resourcemanager.securityinsights.models.MetadataDependenciesAutoGenerated;
import com.azure.resourcemanager.securityinsights.models.MetadataSource;
import com.azure.resourcemanager.securityinsights.models.MetadataSupport;
import com.azure.resourcemanager.securityinsights.models.PackageInstallationProperties;
import com.azure.resourcemanager.securityinsights.models.PackageInstallationPropertiesAuthor;
import com.azure.resourcemanager.securityinsights.models.PackageInstallationPropertiesCategories;
import com.azure.resourcemanager.securityinsights.models.PackageInstallationPropertiesSource;
import com.azure.resourcemanager.securityinsights.models.PackageInstallationPropertiesSupport;
import com.azure.resourcemanager.securityinsights.models.PackageKind;
import com.azure.resourcemanager.securityinsights.models.PackageModel;
import java.time.LocalDate;
import java.util.Collections;
import java.util.List;

public final class PackageModelImpl implements PackageModel, PackageModel.Definition, PackageModel.Update {
    private PackageModelInner innerObject;

    private final com.azure.resourcemanager.securityinsights.SecurityInsightsManager serviceManager;

    public String id() {
        return this.innerModel().id();
    }

    public String name() {
        return this.innerModel().name();
    }

    public String type() {
        return this.innerModel().type();
    }

    public String etag() {
        return this.innerModel().etag();
    }

    public SystemData systemData() {
        return this.innerModel().systemData();
    }

    public String packageId() {
        return this.innerModel().packageId();
    }

    public PackageKind packageKind() {
        return this.innerModel().packageKind();
    }

    public String contentSchemaVersion() {
        return this.innerModel().contentSchemaVersion();
    }

    public Flag isNew() {
        return this.innerModel().isNew();
    }

    public Flag isPreview() {
        return this.innerModel().isPreview();
    }

    public Flag isFeatured() {
        return this.innerModel().isFeatured();
    }

    public String version() {
        return this.innerModel().version();
    }

    public String displayName() {
        return this.innerModel().displayName();
    }

    public String description() {
        return this.innerModel().description();
    }

    public String publisherDisplayName() {
        return this.innerModel().publisherDisplayName();
    }

    public MetadataSource source() {
        return this.innerModel().source();
    }

    public MetadataAuthor author() {
        return this.innerModel().author();
    }

    public MetadataSupport support() {
        return this.innerModel().support();
    }

    public MetadataDependencies dependencies() {
        return this.innerModel().dependencies();
    }

    public List<String> providers() {
        List<String> inner = this.innerModel().providers();
        if (inner != null) {
            return Collections.unmodifiableList(inner);
        } else {
            return Collections.emptyList();
        }
    }

    public LocalDate firstPublishDate() {
        return this.innerModel().firstPublishDate();
    }

    public LocalDate lastPublishDate() {
        return this.innerModel().lastPublishDate();
    }

    public MetadataCategories categories() {
        return this.innerModel().categories();
    }

    public List<String> tags() {
        List<String> inner = this.innerModel().tags();
        if (inner != null) {
            return Collections.unmodifiableList(inner);
        } else {
            return Collections.emptyList();
        }
    }

    public List<String> threatAnalysisTactics() {
        List<String> inner = this.innerModel().threatAnalysisTactics();
        if (inner != null) {
            return Collections.unmodifiableList(inner);
        } else {
            return Collections.emptyList();
        }
    }

    public List<String> threatAnalyticsTechniques() {
        List<String> inner = this.innerModel().threatAnalyticsTechniques();
        if (inner != null) {
            return Collections.unmodifiableList(inner);
        } else {
            return Collections.emptyList();
        }
    }

    public String icon() {
        return this.innerModel().icon();
    }

    public String resourceGroupName() {
        return resourceGroupName;
    }

    public PackageModelInner innerModel() {
        return this.innerObject;
    }

    private com.azure.resourcemanager.securityinsights.SecurityInsightsManager manager() {
        return this.serviceManager;
    }

    private String resourceGroupName;

    private String workspaceName;

    private String packageId;

    private PackageInstallationProperties createPackageInstallationProperties;

    private PackageInstallationProperties updatePackageInstallationProperties;

    public PackageModelImpl withExistingWorkspace(String resourceGroupName, String workspaceName) {
        this.resourceGroupName = resourceGroupName;
        this.workspaceName = workspaceName;
        return this;
    }

    public PackageModel create() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContentPackageOperations()
                .installWithResponse(
                    resourceGroupName, workspaceName, packageId, createPackageInstallationProperties, Context.NONE)
                .getValue();
        return this;
    }

    public PackageModel create(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContentPackageOperations()
                .installWithResponse(
                    resourceGroupName, workspaceName, packageId, createPackageInstallationProperties, context)
                .getValue();
        return this;
    }

    PackageModelImpl(String name, com.azure.resourcemanager.securityinsights.SecurityInsightsManager serviceManager) {
        this.innerObject = new PackageModelInner();
        this.serviceManager = serviceManager;
        this.packageId = name;
        this.createPackageInstallationProperties = new PackageInstallationProperties();
    }

    public PackageModelImpl update() {
        this.updatePackageInstallationProperties = new PackageInstallationProperties();
        return this;
    }

    public PackageModel apply() {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContentPackageOperations()
                .installWithResponse(
                    resourceGroupName, workspaceName, packageId, updatePackageInstallationProperties, Context.NONE)
                .getValue();
        return this;
    }

    public PackageModel apply(Context context) {
        this.innerObject =
            serviceManager
                .serviceClient()
                .getContentPackageOperations()
                .installWithResponse(
                    resourceGroupName, workspaceName, packageId, updatePackageInstallationProperties, context)
                .getValue();
        return this;
    }

    PackageModelImpl(
        PackageModelInner innerObject,
        com.azure.resourcemanager.securityinsights.SecurityInsightsManager serviceManager) {
        this.innerObject = innerObject;
        this.serviceManager = serviceManager;
        this.resourceGroupName = Utils.getValueFromIdByName(innerObject.id(), "resourceGroups");
        this.workspaceName = Utils.getValueFromIdByName(innerObject.id(), "workspaces");
        this.packageId = Utils.getValueFromIdByName(innerObject.id(), "contentPackages");
    }

    public PackageModelImpl withTags(List<String> tags) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withTags(tags);
            return this;
        } else {
            this.updatePackageInstallationProperties.withTags(tags);
            return this;
        }
    }

    public PackageModelImpl withPackageKind(PackageKind packageKind) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withPackageKind(packageKind);
            return this;
        } else {
            this.updatePackageInstallationProperties.withPackageKind(packageKind);
            return this;
        }
    }

    public PackageModelImpl withVersion(String version) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withVersion(version);
            return this;
        } else {
            this.updatePackageInstallationProperties.withVersion(version);
            return this;
        }
    }

    public PackageModelImpl withRelease(String release) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withRelease(release);
            return this;
        } else {
            this.updatePackageInstallationProperties.withRelease(release);
            return this;
        }
    }

    public PackageModelImpl withDisplayName(String displayName) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withDisplayName(displayName);
            return this;
        } else {
            this.updatePackageInstallationProperties.withDisplayName(displayName);
            return this;
        }
    }

    public PackageModelImpl withDescription(String description) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withDescription(description);
            return this;
        } else {
            this.updatePackageInstallationProperties.withDescription(description);
            return this;
        }
    }

    public PackageModelImpl withSource(PackageInstallationPropertiesSource source) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withSource(source);
            return this;
        } else {
            this.updatePackageInstallationProperties.withSource(source);
            return this;
        }
    }

    public PackageModelImpl withAuthor(PackageInstallationPropertiesAuthor author) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withAuthor(author);
            return this;
        } else {
            this.updatePackageInstallationProperties.withAuthor(author);
            return this;
        }
    }

    public PackageModelImpl withSupport(PackageInstallationPropertiesSupport support) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withSupport(support);
            return this;
        } else {
            this.updatePackageInstallationProperties.withSupport(support);
            return this;
        }
    }

    public PackageModelImpl withFirstPublishDate(String firstPublishDate) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withFirstPublishDate(firstPublishDate);
            return this;
        } else {
            this.updatePackageInstallationProperties.withFirstPublishDate(firstPublishDate);
            return this;
        }
    }

    public PackageModelImpl withLastPublishDate(String lastPublishDate) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withLastPublishDate(lastPublishDate);
            return this;
        } else {
            this.updatePackageInstallationProperties.withLastPublishDate(lastPublishDate);
            return this;
        }
    }

    public PackageModelImpl withCategories(PackageInstallationPropertiesCategories categories) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withCategories(categories);
            return this;
        } else {
            this.updatePackageInstallationProperties.withCategories(categories);
            return this;
        }
    }

    public PackageModelImpl withThreatAnalysisTactics(List<String> threatAnalysisTactics) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withThreatAnalysisTactics(threatAnalysisTactics);
            return this;
        } else {
            this.updatePackageInstallationProperties.withThreatAnalysisTactics(threatAnalysisTactics);
            return this;
        }
    }

    public PackageModelImpl withThreatAnalyticsTechniques(List<String> threatAnalyticsTechniques) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withThreatAnalyticsTechniques(threatAnalyticsTechniques);
            return this;
        } else {
            this.updatePackageInstallationProperties.withThreatAnalyticsTechniques(threatAnalyticsTechniques);
            return this;
        }
    }

    public PackageModelImpl withDependencies(MetadataDependenciesAutoGenerated dependencies) {
        if (isInCreateMode()) {
            this.createPackageInstallationProperties.withDependencies(dependencies);
            return this;
        } else {
            this.updatePackageInstallationProperties.withDependencies(dependencies);
            return this;
        }
    }

    private boolean isInCreateMode() {
        return this.innerModel().id() == null;
    }
}
