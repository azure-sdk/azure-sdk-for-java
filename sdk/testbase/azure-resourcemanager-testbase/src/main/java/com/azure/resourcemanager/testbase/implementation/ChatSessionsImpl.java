// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
// Code generated by Microsoft (R) AutoRest Code Generator.

package com.azure.resourcemanager.testbase.implementation;

import com.azure.core.http.rest.PagedIterable;
import com.azure.core.http.rest.Response;
import com.azure.core.http.rest.SimpleResponse;
import com.azure.core.util.Context;
import com.azure.core.util.logging.ClientLogger;
import com.azure.resourcemanager.testbase.fluent.ChatSessionsClient;
import com.azure.resourcemanager.testbase.fluent.models.ChatSessionResourceInner;
import com.azure.resourcemanager.testbase.models.ChatSessionResource;
import com.azure.resourcemanager.testbase.models.ChatSessions;

public final class ChatSessionsImpl implements ChatSessions {
    private static final ClientLogger LOGGER = new ClientLogger(ChatSessionsImpl.class);

    private final ChatSessionsClient innerClient;

    private final com.azure.resourcemanager.testbase.TestBaseManager serviceManager;

    public ChatSessionsImpl(ChatSessionsClient innerClient,
        com.azure.resourcemanager.testbase.TestBaseManager serviceManager) {
        this.innerClient = innerClient;
        this.serviceManager = serviceManager;
    }

    public PagedIterable<ChatSessionResource> list(String resourceGroupName, String testBaseAccountName) {
        PagedIterable<ChatSessionResourceInner> inner
            = this.serviceClient().list(resourceGroupName, testBaseAccountName);
        return Utils.mapPage(inner, inner1 -> new ChatSessionResourceImpl(inner1, this.manager()));
    }

    public PagedIterable<ChatSessionResource> list(String resourceGroupName, String testBaseAccountName,
        Context context) {
        PagedIterable<ChatSessionResourceInner> inner
            = this.serviceClient().list(resourceGroupName, testBaseAccountName, context);
        return Utils.mapPage(inner, inner1 -> new ChatSessionResourceImpl(inner1, this.manager()));
    }

    public Response<ChatSessionResource> getWithResponse(String resourceGroupName, String testBaseAccountName,
        String chatSessionName, Context context) {
        Response<ChatSessionResourceInner> inner
            = this.serviceClient().getWithResponse(resourceGroupName, testBaseAccountName, chatSessionName, context);
        if (inner != null) {
            return new SimpleResponse<>(inner.getRequest(), inner.getStatusCode(), inner.getHeaders(),
                new ChatSessionResourceImpl(inner.getValue(), this.manager()));
        } else {
            return null;
        }
    }

    public ChatSessionResource get(String resourceGroupName, String testBaseAccountName, String chatSessionName) {
        ChatSessionResourceInner inner
            = this.serviceClient().get(resourceGroupName, testBaseAccountName, chatSessionName);
        if (inner != null) {
            return new ChatSessionResourceImpl(inner, this.manager());
        } else {
            return null;
        }
    }

    private ChatSessionsClient serviceClient() {
        return this.innerClient;
    }

    private com.azure.resourcemanager.testbase.TestBaseManager manager() {
        return this.serviceManager;
    }
}
